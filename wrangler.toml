name = "github-mcp-server"
main = "src/worker.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Public vars (non-secret) — secrets are set via: wrangler secret put SECRET_NAME

[[rules]]
type = "ESModule"
globs = ["**/*.ts"]
